import { NextPage } from 'next';
import { ReactElement } from 'react';

const Page: NextPage = (): ReactElement => {
  return <span className="text-red-400 text-3xl">Waduh Ini Landing</span>;
};

export default Page;
