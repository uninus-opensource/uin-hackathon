import { NextPage } from 'next';
import { ReactElement } from 'react';
import { ActivityModule } from '../../_modules/activity';

const ActivityPage: NextPage = (): ReactElement => {
  return <ActivityModule />;
};
export default ActivityPage;
